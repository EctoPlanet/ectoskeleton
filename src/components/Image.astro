---
import type { ImageMetadata } from 'astro';
import { Image } from 'astro:assets';

interface Props {
  imageMetadata: ImageMetadata; // Accept ImageMetadata directly
  altText?: string; // Make altText optional for flexibility
}

const { imageMetadata, altText = '' } = Astro.props; 

// const images = import.meta.glob<ImageMetadata>('/src/assets/images/**/*.{jpeg,jpg,png,gif,webp,svg}');
// const imageModule = Object.keys(images).find(path => path.endsWith(imagePath));
// if (!imageModule) throw new Error(`Image not found: ${imagePath}`);
---

<Image src={imageMetadata} alt={altText} class="rounded-box" />
